//从键盘输入BOY 3个字符，然后把它们输出到屏幕。

#include <stdio.h>
int main ( )                
{ putchar(getchar());           // 将接收到的字符输出 
  putchar(getchar());           // 将接收到的字符输出 
  putchar(getchar());           // 将接收到的字符输出 
  putchar('\n'); 
  return 0;
}